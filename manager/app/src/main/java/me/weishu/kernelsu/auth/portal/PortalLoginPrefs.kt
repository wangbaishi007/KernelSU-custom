package me.weishu.kernelsu.auth.portal

/** Same prefs as [PortalLoginViewModel] (aligned with oss-uploader `login_prefs`). */
object PortalLoginPrefs {
    const val PREFS_NAME = "login_prefs"
    const val KEY_LAST_USERNAME = "last_username"
}
